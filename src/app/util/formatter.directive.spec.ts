import { FormatterDirective } from './formatter.directive';

describe('FormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatterDirective();
    expect(directive).toBeTruthy();
  });
});
