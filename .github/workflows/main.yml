

name: Deploy projeto no dockerhub / integracao com aws

on:
  push:
    branches: 
      - master

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3
            - name: Setup Java
              uses: actions/setup-java@v3
              with:
                distribution: 'temurin'
                java-version: '8'
            - name: Build project
              working-directory: backend/
              run: mvn clean install -DskipTests
            - name: Login Docker Hub
              run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
            - name: Build docker image
              run: docker build -t timbeck1997/finnanceapp ./backend/
            - name: Push image docker
              run: docker push timbeck1997/finnanceapp
              
    # deploy:
    #     needs: build
    #     runs-on: self-hosted
    #     steps:
    #         - name: Pull image from docker hub
    #           run: docker pull timbeck1997/finnanceapp:latest
    #         - name: Remove docker container
    #           run: docker rm -f finnanceapp
    #         - name: Run docker container
    #           run: docker run -d -p 8080:8080  --name deploy_ghacfinnanceapptions timbeck1997/finnanceapp

   
